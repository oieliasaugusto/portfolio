import styled from "styled-components";

export const NomeiaClasse = styled.button`
border-radius: 10px;
`
// Lembre-se: o CSS fica dentro de ``
// E a tag funcional é inserida após "= styled."
// "styled.button", "styled.section", "styled.img"
// Bons estudos!!